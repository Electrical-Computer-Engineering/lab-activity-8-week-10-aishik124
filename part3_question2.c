#include <stdio.h>
#include <string.h>
#define MAX 11

int main()
{
	printf ("s1: ");
		scanf ("%s", s1); getchar();
		
		printf ("s2: ");
		scanf ("%s", s2); getchar();
		
	for(i=0;i<strlen(s1);i++)
	{
		for(j=0;j<strlen(s2);j++)	
		{
			if(str)
		}
	}
}
